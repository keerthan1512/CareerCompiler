[project]
name = "career-compiler-matching-engine"
version = "0.1.0"
description = "CareerCompiler Evidence Matching and Tailoring Engine"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
